apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-go-service-a
  namespace: applications
  labels:
    app.kubernetes.io/name: simple-go-service-a
    app.kubernetes.io/version: latest
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: simple-go-service-a
  template:
    metadata:
      labels:
        app.kubernetes.io/name: simple-go-service-a
        app.kubernetes.io/version: latest
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: simple-go-service-a
          image: ghcr.io/krech-et/simple-go-service-a:latest
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
